# Horizontal Pod Autoscaler

HPA automatically scales the number of pod replicas up or down based on CPU or memory usage. When demand increases, HPA adds replicas to handle the load. When demand drops, it removes them to free up resources.

Configure HPA for any workload that experiences variable traffic and needs to scale without manual intervention.
